spring:
  application:
    name: backend
  profiles:
    default: local

server:
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
